{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["bom1"],
  "functions": {
    "app/main.py": {
      "maxDuration": 30,
      "excludeFiles": "{tests/**,alembic/**,scripts/**,.venv/**,**/test_*.py,Dockerfile}"
    }
  }
}
